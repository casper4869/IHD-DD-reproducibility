# Figure 4 visual review

> **Scope:** Post-submission internal audit/sensitivity only. This file does not describe or replace the manuscript Figure 4.


The maker opened the audit-only R-generated PNG at full preview resolution after adding the instability/clipping note to panel a. Text, axes, panel titles, annotations, endpoint labels and the shared legend are readable, with no visible overlap or clipping. Both panels use the same x/y limits and a zero count origin. The historical series is repeated identically; colors plus linetypes/shapes distinguish models. The text visibly identifies original VARX instability and clipping, the 2026 onset of unavailable full-denominator counts, the constant-SDI scenario and absence of uncertainty intervals.

Exports are 183 x 103 mm; the raster export uses 600 dpi and LZW TIFF compression, and the preview uses 300 dpi PNG. SVG retains text elements; PDF uses an Arial-compatible Cairo device. R produced every graphic and raster preview. No image retouching occurred. Quantitative geometry checks confirm identical axis ranges and no GeomRibbon layers. Connector-specific gates are inapplicable to this line-chart grid. No DOCX was edited by this subtask; document rendering belongs to the manuscript integration task.

This visual pass does not validate the underlying original VARX forecasts. The artifact explicitly reports a failed stability premise and is retained only as an audit/sensitivity record, not as the manuscript Figure 4. Independent review is recorded separately.
